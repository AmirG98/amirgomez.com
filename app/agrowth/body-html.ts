// Auto-generated from the A+ Growth static build (scripts included).
// Do not edit by hand; regenerate from index.html if the design changes.
const bodyHtml: string = "  <a class=\"skip-link\" href=\"#main-content\">Skip to main content</a>\n  <canvas id=\"gl\" aria-hidden=\"true\"></canvas>\n  <canvas id=\"tail-liquid\" aria-hidden=\"true\"></canvas>\n  <div class=\"scrim\" aria-hidden=\"true\"></div>\n  <div class=\"cursor-haze\" aria-hidden=\"true\"></div>\n  <div class=\"grain\" aria-hidden=\"true\"></div>\n\n  <main id=\"main-content\" class=\"content\" tabindex=\"-1\">\n    <nav>\n      <span class=\"brand\">A<span class=\"accent\">+</span> GROWTH</span>\n      <span class=\"links\">\n        <a href=\"#signal\">Evidence</a><a href=\"#method\">Method</a><a href=\"#work\">Cases</a><a href=\"#team\">Team</a><a href=\"#contact\">Contact</a>\n      </span>\n    </nav>\n\n    <header class=\"hero\">\n      <span class=\"mono eyebrow\">A+ Growth — Predictable Revenue Growth</span>\n      <h1>Turn ad spend into<br><span class=\"accent\">predictable revenue.</span></h1>\n      <p class=\"sub\">We turn scattered paid media, funnels and analytics into one clean acquisition signal.</p>\n      <p class=\"hero-fit\">For teams already investing in acquisition but lacking a clear signal between creative, funnel and revenue.</p>\n      <a class=\"cta\" href=\"#contact\" data-event=\"hero_audit_click\"><span>Request a growth audit</span><span class=\"arrow\">→</span></a>\n      <div class=\"hero-proof\" aria-label=\"Experience and reach\">\n        <span>10 years</span><span>300+ funnels</span><span>US / Europe / LatAm</span>\n        <a href=\"#signal\" data-event=\"hero_evidence_click\">See the evidence ↓</a>\n      </div>\n    </header>\n    <span class=\"mono scroll-cue\">scroll ↓</span>\n\n    <section class=\"lead-in\" aria-label=\"Intro\">\n      <span class=\"mono\">The problem</span>\n      <p>Most teams scale spend before the funnel is ready — pouring budget into what they can't measure. We calibrate demand first, so every dollar follows a clear signal from creative to revenue. Here's the evidence.</p>\n    </section>\n    <section id=\"signal\" class=\"signal-cloud\" aria-label=\"Results\">\n      <div class=\"stage\" data-cloud=\"group\">\n        <div class=\"stage-copy cloud-copy\">\n          <span class=\"mono panel-eyebrow\">Margin expansion</span>\n          <h2 class=\"sr-only\">$1M+ in margin from $2,744 in ad spend.</h2>\n          <p>$1M+ in margin from just $2,744 in ad spend — one webinar funnel, still live.</p>\n        </div>\n        <div class=\"stage-copy cloud-copy\">\n          <span class=\"mono panel-eyebrow\">Cost efficiency</span>\n          <h2 class=\"sr-only\">10× lower cost per lead.</h2>\n          <p>10× lower cost per qualified lead — from $50 down to $5.</p>\n        </div>\n        <div class=\"stage-copy cloud-copy\">\n          <span class=\"mono panel-eyebrow\">Creative scaling</span>\n          <h2 class=\"sr-only\">7× ROAS.</h2>\n          <p>7× ROAS after scaling UGC + AI creative — up from 2×.</p>\n        </div>\n        <div class=\"stage-copy cloud-copy\">\n          <span class=\"mono panel-eyebrow\">Lead volume</span>\n          <h2 class=\"sr-only\">63,000 leads.</h2>\n          <p>63,000 leads generated at $4.5 average cost — a single social funnel.</p>\n        </div>\n      </div>\n    </section>\n\n    <section id=\"method\" aria-label=\"Method\">\n      <div class=\"stage\" data-stage=\"0\">\n        <div class=\"stage-copy method-copy\">\n          <span class=\"mono case-name\">Step 01</span>\n          <h2 class=\"sr-only\">Understand the system, then create.</h2>\n          <p>Deep discovery on the business model, audience and funnel assets, then creative built at volume — ads varied up to 12× more often than standard, always on-brand.</p>\n        </div>\n      </div>\n      <div class=\"stage\" data-stage=\"1\">\n        <div class=\"stage-copy method-copy\">\n          <span class=\"mono case-name\">Step 02</span>\n          <h2 class=\"sr-only\">Ship the funnel, then measure everything.</h2>\n          <p>Instrument the funnel, then ship fast. Every decision ties back to a measurable outcome, so budget only follows what moves revenue.</p>\n        </div>\n      </div>\n      <div class=\"stage\" data-stage=\"2\">\n        <div class=\"stage-copy method-copy\">\n          <span class=\"mono case-name\">Step 03</span>\n          <h2 class=\"sr-only\">The signal compounds.</h2>\n          <p>Winners earn budget; weak signals are cut. The data shows what to scale next — compounding every week the funnel keeps running.</p>\n        </div>\n      </div>\n    </section>\n\n    <section id=\"work\" aria-label=\"Work\">\n      <div class=\"stage\" data-pair=\"0\">\n        <div class=\"stage-copy case-copy\">\n          <span class=\"mono panel-eyebrow\">Close &amp; Convert</span>\n          <span class=\"mono case-name\">Webinar + VSL</span>\n          <h2 class=\"sr-only\">$1M+ in margin from $2,744 in ad spend.</h2>\n          <p>$1M+ in margin — a B2B/B2C lending funnel with on-demand webinars, 5 deals closed from one campaign.</p>\n        </div>\n        <div class=\"stage-copy case-copy\">\n          <span class=\"mono panel-eyebrow\">Close &amp; Convert</span>\n          <span class=\"mono case-name\">Lead Magnet B2B</span>\n          <h2 class=\"sr-only\">10× lower cost per lead.</h2>\n          <p>10× lower cost per lead — full funnel and lead magnet built end to end, creative varied up to 12× more often.</p>\n        </div>\n      </div>\n      <div class=\"stage\" data-pair=\"1\">\n        <div class=\"stage-copy case-copy\">\n          <span class=\"mono panel-eyebrow\">Creative &amp; Volume</span>\n          <span class=\"mono case-name\">UGC + AI</span>\n          <h2 class=\"sr-only\">7× ROAS, up from 2×.</h2>\n          <p>7× ROAS, up from 2× — 20× more UGC volume for a US legal company, AI-assisted production made it possible.</p>\n        </div>\n        <div class=\"stage-copy case-copy\">\n          <span class=\"mono panel-eyebrow\">Creative &amp; Volume</span>\n          <span class=\"mono case-name\">LeadGen B2C</span>\n          <h2 class=\"sr-only\">63,000 leads at $4.5 average cost.</h2>\n          <p>63,000 leads at $4.5 each — a $335K/yr social campaign for a home-decor leader, built and scaled end to end.</p>\n        </div>\n      </div>\n      <div class=\"stage\" data-pair=\"2\">\n        <div class=\"stage-copy case-copy\">\n          <span class=\"mono panel-eyebrow\">Channel Mastery</span>\n          <span class=\"mono case-name\">PPC B2B</span>\n          <h2 class=\"sr-only\">3.7× revenue growth.</h2>\n          <p>3.7× revenue growth — Google and Microsoft Ads over 18 months, ROI doubled with a TOFU/BOFU split.</p>\n        </div>\n        <div class=\"stage-copy case-copy\">\n          <span class=\"mono panel-eyebrow\">Channel Mastery</span>\n          <span class=\"mono case-name\">SEO + GEO</span>\n          <h2 class=\"sr-only\">5.3× organic traction.</h2>\n          <p>5.3× organic traction — SEO and GEO for a Texas trailer-industry leader, 410,000+ organic visitors in 2024.</p>\n        </div>\n      </div>\n    </section>\n\n    <section class=\"expertise\" aria-labelledby=\"expertise-title\">\n      <div class=\"expertise-head\">\n        <div>\n          <span class=\"mono\">Ten years in market</span>\n          <h2 id=\"expertise-title\">Ten years. 300+ funnels.<br>Every channel that moves revenue.</h2>\n        </div>\n        <p>Wherever growth stalls, we've seen it before — and fixed it. Here's what we solve, and who we solve it for.</p>\n      </div>\n\n      <div class=\"expertise-solve\">\n        <span class=\"mono expertise-col-label\">What we solve</span>\n        <ul class=\"expertise-list\">\n          <li>Traffic that doesn't convert</li>\n          <li>Creative that stopped scaling</li>\n          <li>Leads that never close</li>\n          <li>Cost per lead climbing</li>\n          <li>Funnels leaking between steps</li>\n          <li>Spend you can't attribute</li>\n        </ul>\n      </div>\n\n      <div class=\"expertise-industries\">\n        <span class=\"mono expertise-col-label\">Who we serve</span>\n        <ul class=\"expertise-grid\">\n          <li>Lending</li>\n          <li>Legal</li>\n          <li>Home &amp; furnishings</li>\n          <li>Home-decor</li>\n          <li>Auto &amp; auto-parts</li>\n          <li>Trailer &amp; outdoor / RV</li>\n          <li>Real estate</li>\n          <li>Financial services</li>\n          <li>Construction funding</li>\n          <li>Healthcare</li>\n          <li>Consumer products / e-commerce</li>\n          <li>SaaS &amp; tech</li>\n          <li>Education &amp; coaching</li>\n          <li>Med/dental &amp; wellness</li>\n        </ul>\n      </div>\n    </section>\n\n    <section id=\"team\" class=\"team\" aria-label=\"Team\">\n      <h2 class=\"mono team-eyebrow\">The lead behind the team</h2>\n      <div class=\"team-solo\">\n        <div class=\"member lead\">\n          <img src=\"/agrowth/amir.jpg\" alt=\"Amir Gómez\" width=\"640\" height=\"640\" loading=\"lazy\" decoding=\"async\">\n        </div>\n        <div class=\"team-solo-copy\">\n          <h3>Amir Gómez</h3>\n          <span class=\"mono role\">Founder &amp; Growth Lead</span>\n          <p>Ten years and 300+ funnels in, Amir sets the strategy and runs point on every account — with a senior team of paid media, creative and data specialists executing behind him. One standard holds it together: revenue moved, not hours logged.</p>\n          <dl class=\"team-stats\" aria-label=\"Track record\">\n            <div><dt class=\"mono\">Experience</dt><dd>10 yrs</dd></div>\n            <div><dt class=\"mono\">Funnels built</dt><dd>300+</dd></div>\n            <div><dt class=\"mono\">Markets</dt><dd>US · EU · LatAm</dd></div>\n          </dl>\n          <p class=\"mono team-note\">Strategy, creative and data stay in the same room.</p>\n        </div>\n      </div>\n    </section>\n\n    <section id=\"contact\" class=\"contact\" aria-label=\"Contact\">\n      <span class=\"mono contact-eyebrow\">Ready to calibrate?</span>\n      <h2>Let’s find what your growth budget should scale next.</h2>\n      <p class=\"contact-intro\">Tell us what’s underperforming, where you want to grow, and what the numbers look like today.</p>\n      <div class=\"contact-actions\">\n        <a class=\"cta\" data-event=\"contact_audit_click\" href=\"mailto:amir@amirgomez.com?subject=Growth%20audit%20request&amp;body=Website%3A%0AWhat%27s%20underperforming%3A%0AWhere%20we%20want%20to%20grow%3A%0AWhat%20the%20numbers%20look%20like%20today%3A\"><span>Request a growth audit</span><span class=\"arrow\">→</span></a>\n        <a class=\"mono contact-alt\" data-event=\"whatsapp_click\" href=\"https://wa.me/5493541370209\" target=\"_blank\" rel=\"noopener\">or WhatsApp +54 9 3541 370209</a>\n      </div>\n    </section>\n  </main>\n\n  <footer class=\"site-footer\">\n    <span class=\"mono\">A+ GROWTH</span>\n    <span class=\"mono\">amirgomez.com</span>\n  </footer>\n\n  <script>\n    window.setTimeout(()=>{\n      if(!document.documentElement.classList.contains('webgl-ready')) document.documentElement.classList.add('webgl-failed');\n    },8000);\n  </script>\n\n  <script type=\"importmap\">\n  { \"imports\": { \"three\": \"https://cdn.jsdelivr.net/npm/three@0.160.0/build/three.module.js\",\n    \"three/addons/\": \"https://cdn.jsdelivr.net/npm/three@0.160.0/examples/jsm/\" } }\n  </script>\n  <script type=\"module\">\n  const [THREE,\n    {EffectComposer},{RenderPass},{UnrealBloomPass},{SMAAPass},{OutputPass},\n    {RoundedBoxGeometry},{FontLoader},{TextGeometry},{mergeGeometries}\n  ]=await Promise.all([\n    import('three'),\n    import('three/addons/postprocessing/EffectComposer.js'),\n    import('three/addons/postprocessing/RenderPass.js'),\n    import('three/addons/postprocessing/UnrealBloomPass.js'),\n    import('three/addons/postprocessing/SMAAPass.js'),\n    import('three/addons/postprocessing/OutputPass.js'),\n    import('three/addons/geometries/RoundedBoxGeometry.js'),\n    import('three/addons/loaders/FontLoader.js'),\n    import('three/addons/geometries/TextGeometry.js'),\n    import('three/addons/utils/BufferGeometryUtils.js')\n  ]);\n  await new Promise(resolve=>requestAnimationFrame(()=>requestAnimationFrame(resolve)));\n\n  const REDUCE=matchMedia('(prefers-reduced-motion:reduce)').matches;\n  document.documentElement.classList.toggle('reduced-motion',REDUCE);\n  const TOUCH=matchMedia('(pointer:coarse)').matches;\n  const TIER=(()=>{\n    const small=Math.min(innerWidth,innerHeight)<760;\n    if(small) return{dpr:Math.min(devicePixelRatio,1.25),count:2400,quality:'low',blurTaps:5,bloom:0};\n    if(TOUCH) return{dpr:Math.min(devicePixelRatio,1.75),count:8000,quality:'mid',blurTaps:3,bloom:0.28};\n    return{dpr:Math.min(devicePixelRatio,2),count:9000,quality:'high',blurTaps:5,bloom:0.32};})();\n  const CAM_RESPONSE=TIER.quality==='low'?14:5.0;\n  // Side-by-side dials need BOTH a wide-enough aspect ratio AND enough absolute\n  // width — otherwise tall/narrow viewports (~768-1100px) render two big dials\n  // side by side that don't fit, clipping the 3D text at the edges. Keep this in\n  // sync with the CSS `max-width:900px` breakpoint below (900px itself = stacked).\n  const isPairSide=()=>(innerWidth/innerHeight)>=0.75 && innerWidth>900;\n  const PAIR_SIDE=isPairSide();\n  if(!PAIR_SIDE) document.body.classList.add('pairs-stack');\n\n  const canvas=document.getElementById('gl');\n  const renderer=new THREE.WebGLRenderer({canvas,antialias:true});\n  renderer.setPixelRatio(TIER.dpr); renderer.setClearColor(0x050608,1);\n  renderer.toneMapping=THREE.ACESFilmicToneMapping; renderer.toneMappingExposure=0.9;\n  const scene=new THREE.Scene();\n  const camera=new THREE.PerspectiveCamera(46,innerWidth/innerHeight,0.1,100); camera.position.set(0,0,9*aspectZScale());\n\n  const CLOUD_Y=-9;\n  const CLOUD_STATS=[\n    {hook:'$1M+', label:'Margin expansion', color:0xe8551e, dx:-1.8, dy: 1.2,  caption:'In margin from $2,744 in ad spend.'},\n    {hook:'10X',  label:'Cost efficiency', color:0xf0611f, dx: 1.7, dy: 1.0,  caption:'Lower cost per lead — $50 down to $5.'},\n    {hook:'7X',   label:'Creative scaling', color:0xff6a2b, dx:-1.4, dy:-2.0,  caption:'ROAS from UGC + AI creative, up from 2×.'},\n    {hook:'63K',  label:'Lead volume', color:0xff7a3d, dx: 2.0, dy:-2.0,  caption:'Leads at $4.5 average cost.'},\n  ].map(s=>Object.assign({},s,{x:s.dx, y:CLOUD_Y+s.dy}));\n  if(!PAIR_SIDE) CLOUD_STATS.forEach((s,i)=>{s.x=0;s.y=CLOUD_Y+1.2-i*3.8});\n\n  const STAGES=[\n    {y:PAIR_SIDE?-15:-24, x:-0.7, color:0x6b645c, lines:['UNDERSTAND','& CREATE'], step:'Step 01',\n      caption:'Discovery on model, audience and funnel — then creative built at volume, on-brand.'},\n    {y:PAIR_SIDE?-20:-29, x: 0.7, color:0xe8551e, lines:['SHIP &','MEASURE'], step:'Step 02',\n      caption:'Instrument the funnel, ship fast — every decision tied to a measurable outcome.'},\n    {y:PAIR_SIDE?-25:-34, x:-0.5, color:0xff7a3d, lines:['THE SIGNAL','COMPOUNDS'], step:'Step 03',\n      caption:'Winners earn budget, weak signals get cut — compounding every week.'},\n  ];\n  if(!PAIR_SIDE)STAGES.forEach(s=>{s.x=0});\n  const CASE_PAIRS=[\n    { theme:'Close & Convert', cases:[\n      {color:0xf0611f, hook:'$1M+', name:'Webinar + VSL',\n        caption:'B2B/B2C lending funnel — 5 deals from one campaign.'},\n      {color:0xf0611f, hook:'10X', name:'Lead Magnet B2B',\n        caption:'Full funnel rebuilt — creative varied up to 12× more often.'},\n    ]},\n    { theme:'Creative & Volume', cases:[\n      {color:0xff7a3d, hook:'7X', name:'UGC + AI',\n        caption:'20× more UGC for a US legal company — AI-assisted.'},\n      {color:0xff7a3d, hook:'63K', name:'LeadGen B2C',\n        caption:'$335K/yr social campaign for a home-decor leader.'},\n    ]},\n    { theme:'Channel Mastery', cases:[\n      {color:0xff9a68, hook:'3.7X', name:'PPC B2B',\n        caption:'Google & Microsoft Ads over 18 months — ROI doubled.'},\n      {color:0xff9a68, hook:'5.3X', name:'SEO + GEO',\n        caption:'SEO + GEO for a Texas trailer leader — 410K+ visitors in 2024.'},\n    ]},\n  ];\n  const PAIR_GAP=PAIR_SIDE?5:7;\n  const CASES=[];\n  CASE_PAIRS.forEach((pair,gi)=>{\n    const groupY=STAGES.at(-1).y-4-gi*PAIR_GAP;\n    pair.cases.forEach((c,ci)=>{\n      CASES.push(Object.assign({},c,{\n        theme: pair.theme,\n        y: PAIR_SIDE ? groupY : STAGES.at(-1).y-4-(gi*2+ci)*2.55,\n        x: PAIR_SIDE ? (ci===0?-1.6:1.6) : 0,\n      }));\n    });\n  });\n  if(!PAIR_SIDE)CASES[5].y=CASES[3].y;\n\n\n  const THREAD_TOP_Y=18;\n  const threadPts=[new THREE.Vector3(0,THREAD_TOP_Y,-0.4)];\n  threadPts.push(new THREE.Vector3(0, CLOUD_Y, -0.4));   // el hilo también pasa por la nube de señal\n  STAGES.forEach(s=>threadPts.push(new THREE.Vector3(s.x*0.5, s.y, -0.4)));\n  CASE_PAIRS.forEach((pair,gi)=>{\n    const pairCases=CASES.slice(gi*2,gi*2+2);\n    threadPts.push(new THREE.Vector3(0,(pairCases[0].y+pairCases[1].y)*.5,-0.4));\n  });\n  threadPts.push(new THREE.Vector3(0, CASES.at(-1).y-8, -0.4));\n  const threadCurve=new THREE.CatmullRomCurve3(threadPts);\n  const TUBE_TOP=THREAD_TOP_Y, TUBE_BOTTOM=CASES.at(-1).y-8;\n\n  const N=TIER.count;\n  const center=new Float32Array(N*3), basisA=new Float32Array(N*3), basisB=new Float32Array(N*3);\n  const chaosOff=new Float32Array(N*3), angle=new Float32Array(N), rnd=new Float32Array(N);\n  const bigZ=new Float32Array(N);   // ~3%: partículas grandes que flotan ADELANTE de las placas (sutil, detalle de profundidad)\n  const _up=new THREE.Vector3(0,1,0), _alt=new THREE.Vector3(1,0,0);\n  const _p=new THREE.Vector3(), _tan=new THREE.Vector3(), _bin=new THREE.Vector3(), _nrm=new THREE.Vector3();\n  for(let i=0;i<N;i++){\n    const t=i/(N-1);\n    threadCurve.getPointAt(t,_p); threadCurve.getTangentAt(t,_tan);\n    const useUp=Math.abs(_tan.dot(_up))>0.98?_alt:_up;\n    _bin.crossVectors(useUp,_tan).normalize();\n    _nrm.crossVectors(_tan,_bin);\n    const radius=0.55-t*0.32;                        // se angosta: nube ancha arriba → señal fina abajo\n    center[i*3]=_p.x; center[i*3+1]=_p.y; center[i*3+2]=_p.z;\n    basisA[i*3]=_nrm.x*radius; basisA[i*3+1]=_nrm.y*radius; basisA[i*3+2]=_nrm.z*radius;\n    basisB[i*3]=_bin.x*radius; basisB[i*3+1]=_bin.y*radius; basisB[i*3+2]=_bin.z*radius;\n    angle[i]=(i%23)*0.27+t*Math.PI*6;\n    const cr=(1.4+Math.random()*2.6)*(1.25-t*0.8);\n    const ca=Math.random()*Math.PI*2, cz=(Math.random()-0.5)*Math.PI;\n    chaosOff[i*3]  =Math.cos(ca)*Math.cos(cz)*cr;\n    chaosOff[i*3+1]=Math.sin(cz)*cr*0.65;\n    chaosOff[i*3+2]=Math.sin(ca)*Math.cos(cz)*cr;\n    rnd[i]=Math.random();\n    bigZ[i]=(i%70===0)?(0.15+Math.random()*0.18):0;   // ~1.4%, máx 0.33 (antes 3%, máx 0.9)\n  }\n  const geo=new THREE.BufferGeometry();\n  geo.setAttribute('position',new THREE.BufferAttribute(center.slice(),3));\n  geo.setAttribute('aCenter',new THREE.BufferAttribute(center,3));\n  geo.setAttribute('aBasisA',new THREE.BufferAttribute(basisA,3));\n  geo.setAttribute('aBasisB',new THREE.BufferAttribute(basisB,3));\n  geo.setAttribute('aChaosOff',new THREE.BufferAttribute(chaosOff,3));\n  geo.setAttribute('aAngle',new THREE.BufferAttribute(angle,1));\n  geo.setAttribute('aRnd',new THREE.BufferAttribute(rnd,1));\n  geo.setAttribute('aBigZ',new THREE.BufferAttribute(bigZ,1));\n\n  const uniforms={ uTime:{value:0}, uOrder:{value:0.0}, uMouse:{value:new THREE.Vector3(99,99,99)}, uForce:{value:0}, uReadFocus:{value:0} };\n  const uSpin={value:0.5}, uTop={value:TUBE_TOP}, uSpan={value:TUBE_TOP-TUBE_BOTTOM};\n  const mat=new THREE.ShaderMaterial({ uniforms:Object.assign(uniforms,{uSpin,uTop,uSpan}), transparent:true, depthWrite:false, blending:THREE.NormalBlending,\n    vertexShader:`\n      uniform float uTime,uOrder,uSpin,uTop,uSpan; uniform vec3 uMouse; uniform float uForce;\n      attribute vec3 aCenter,aBasisA,aBasisB,aChaosOff; attribute float aAngle,aRnd,aBigZ; varying float vOrder,vTwinkle;\n      float hash(vec3 p){ return fract(sin(dot(p,vec3(127.1,311.7,74.7)))*43758.5); }\n      float noise(vec3 p){ vec3 i=floor(p),f=fract(p); f=f*f*(3.-2.*f);\n        float n=mix(mix(mix(hash(i),hash(i+vec3(1,0,0)),f.x),mix(hash(i+vec3(0,1,0)),hash(i+vec3(1,1,0)),f.x),f.y),\n                    mix(mix(hash(i+vec3(0,0,1)),hash(i+vec3(1,0,1)),f.x),mix(hash(i+vec3(0,1,1)),hash(i+vec3(1,1,1)),f.x),f.y),f.z);\n        return n; }\n      vec3 curl(vec3 p){ float e=0.2;\n        float x=noise(p+vec3(0,e,0))-noise(p-vec3(0,e,0));\n        float y=noise(p+vec3(0,0,e))-noise(p-vec3(0,0,e));\n        float z=noise(p+vec3(e,0,0))-noise(p-vec3(e,0,0));\n        return normalize(vec3(y-z,z-x,x-y)+1e-4); }\n      void main(){\n        float yN=(aCenter.y-(uTop-uSpan))/uSpan;         // 0 en la última placa, 1 en el hero\n        float order=clamp(uOrder*1.5 - yN*0.9 + 0.35, 0.0, 1.0);  // abajo (yN chico) se ordena primero\n        vOrder=order;\n        float ang=aAngle+uTime*uSpin;\n        vec3 orderedPos=aCenter+aBasisA*cos(ang)+aBasisB*sin(ang);\n        vec3 turb=curl(aCenter*0.22+uTime*0.08)*1.6;\n        vec3 chaosPos=aCenter+aChaosOff+turb;\n        vec3 pos=mix(chaosPos, orderedPos, order);\n        pos.z+=aBigZ+sin(uTime*0.4+aAngle)*aBigZ*0.3;\n        vec3 tm=pos-uMouse; float d=length(tm);\n        pos+=normalize(tm+1e-4)*uForce*smoothstep(2.6,0.0,d);\n        float twinklePhase=uTime*0.5+aRnd*62.83+aAngle*1.7;\n        vTwinkle=pow(max(0.0,sin(twinklePhase)),16.0)*${(TIER.quality==='low'?0:1).toFixed(1)};\n        vec4 mv=modelViewMatrix*vec4(pos,1.0);\n        gl_PointSize=(aRnd*1.0+0.35)*(52.0/-mv.z)*(0.6+order*0.6)*(1.0+aBigZ*2.2)*(1.0+vTwinkle*0.4)*${(TIER.quality==='low'?2.25:1).toFixed(2)};\n        gl_Position=projectionMatrix*mv;\n      }`,\n    fragmentShader:`\n      uniform float uReadFocus; varying float vOrder,vTwinkle;\n      void main(){\n        vec2 uv=gl_PointCoord-0.5; float d=length(uv);\n        if(d>0.5) discard;\n        float a=smoothstep(0.5,0.0,d);\n        vec3 chaosCol=vec3(0.58,0.53,0.49);   // demanda cruda: gris cálido apagado\n        vec3 signalCol=vec3(${TIER.quality==='low'?'1.65,0.60,0.22':'1.45,0.49,0.17'});  // naranja HDR; mobile compensa la ausencia de bloom multipass\n        vec3 col=mix(chaosCol,signalCol,vOrder);\n        col+=vTwinkle*vec3(1.1,0.9,0.6);       // destello: pico HDR por encima de 1.0 (el bloom lo agarra), moderado\n        float readingFade=1.0-uReadFocus*${(TIER.quality==='low'?.2:.6).toFixed(2)};\n        float hotCore=smoothstep(0.22,0.0,d);\n        col+=hotCore*vec3(${TIER.quality==='low'?'0.72,0.34,0.15':'0.38,0.22,0.11'});\n        gl_FragColor=vec4(col*readingFade, min(1.0,a+hotCore*0.22+vTwinkle*0.3)*readingFade);\n      }`});\n  const points=new THREE.Points(geo,mat); scene.add(points);\n\n  function makeMatcap(){\n    const c=document.createElement('canvas'); c.width=c.height=256; const x=c.getContext('2d');\n    const g=x.createRadialGradient(128,110,18,128,128,150);\n    g.addColorStop(0,'#f0e6dc'); g.addColorStop(.42,'#7d6a5c'); g.addColorStop(.72,'#241a15'); g.addColorStop(1,'#0a0705');\n    x.fillStyle=g; x.fillRect(0,0,256,256);\n    const hi=x.createRadialGradient(94,80,1,94,80,46);\n    hi.addColorStop(0,'rgba(255,255,255,.8)'); hi.addColorStop(.55,'rgba(245,232,220,.25)'); hi.addColorStop(1,'transparent');\n    x.fillStyle=hi; x.fillRect(0,0,256,256);\n    const t=new THREE.CanvasTexture(c); t.colorSpace=THREE.SRGBColorSpace; return t;\n  }\n  function frostGlass(matcap,color,opts){\n    const matcapWeight=((opts&&opts.matcapWeight)||(TIER.quality==='low'?0.16:0.20)).toFixed(3);\n    const neonStrength=((opts&&opts.neonStrength!=null)?opts.neonStrength:1).toFixed(3);\n    const uHover={value:0};\n    const m=new THREE.MeshMatcapMaterial({matcap,color:0xffffff,transparent:true,\n      opacity:TIER.quality==='low'?0.38:0.44,depthWrite:false});\n    const taps=TIER.blurTaps;\n    const behindCode = taps>=5 ? `\n        vec3 behind=texture2D(tBackdrop,distortedUV).rgb*0.08;\n        behind+=texture2D(tBackdrop,distortedUV+vec2(blurAmt,0.0)).rgb*0.13;\n        behind+=texture2D(tBackdrop,distortedUV-vec2(blurAmt,0.0)).rgb*0.13;\n        behind+=texture2D(tBackdrop,distortedUV+vec2(0.0,blurAmt)).rgb*0.13;\n        behind+=texture2D(tBackdrop,distortedUV-vec2(0.0,blurAmt)).rgb*0.13;\n        vec2 diagBlur=vec2(blurAmt*0.70);\n        behind+=texture2D(tBackdrop,distortedUV+diagBlur).rgb*0.10;\n        behind+=texture2D(tBackdrop,distortedUV-diagBlur).rgb*0.10;\n        behind+=texture2D(tBackdrop,distortedUV+vec2(diagBlur.x,-diagBlur.y)).rgb*0.10;\n        behind+=texture2D(tBackdrop,distortedUV+vec2(-diagBlur.x,diagBlur.y)).rgb*0.10;\n    ` : taps>=3 ? `\n        vec3 behind=texture2D(tBackdrop,distortedUV).rgb*0.34;\n        behind+=texture2D(tBackdrop,distortedUV+vec2(blurAmt,0.0)).rgb*0.33;\n        behind+=texture2D(tBackdrop,distortedUV-vec2(blurAmt,0.0)).rgb*0.33;\n    ` : `\n        vec3 behind=texture2D(tBackdrop,distortedUV).rgb;\n    `;\n    m.onBeforeCompile=(sh)=>{\n      sh.uniforms.uHover=uHover;\n      sh.uniforms.uNeon={value:new THREE.Color(color)};\n      sh.uniforms.tBackdrop={value:null};\n      sh.uniforms.uResolution={value:new THREE.Vector2(1,1)};\n      sh.uniforms.uViewportResolution={value:new THREE.Vector2(1,1)};\n      m.userData.shader=sh;   // guardar referencia para actualizar tBackdrop/uResolution cada frame\n      sh.fragmentShader='uniform float uHover; uniform vec3 uNeon; uniform sampler2D tBackdrop; uniform vec2 uResolution; uniform vec2 uViewportResolution;\\n'+sh.fragmentShader;\n      sh.fragmentShader=sh.fragmentShader.replace(\n        'vec3 outgoingLight = diffuseColor.rgb * matcapColor.rgb;',\n        `\n        float fres=pow(1.0-max(dot(normal,viewDir),0.0),2.5);\n        float jHash=fract(sin(dot(gl_FragCoord.xy,vec2(12.9898,78.233)))*43758.5453);\n        float distortAmt=${(TIER.quality==='low'?0:.06).toFixed(3)}+fres*${(TIER.quality==='low'?0:.1).toFixed(3)};\n        vec2 screenUV=gl_FragCoord.xy/uViewportResolution;\n        vec2 distortedUV=clamp(screenUV+(normal.xy+vec2(jHash-0.5)*${(TIER.quality==='low'?.06:.15).toFixed(3)})*distortAmt, 0.002, 0.998);\n        float blurAmt=${(TIER.quality==='low'?.040:.018).toFixed(3)}+fres*${(TIER.quality==='low'?.046:.024).toFixed(3)};\n        ${behindCode}\n        behind*=${(TIER.quality==='low'?1.56:1.42).toFixed(2)};\n        float frostLuma=dot(behind,vec3(0.299,0.587,0.114));\n        behind=mix(behind,vec3(frostLuma)*vec3(1.08,0.99,0.91),${(TIER.quality==='low'?.42:.35).toFixed(2)});\n        float frostVeil=${(TIER.quality==='low'?.17:.15).toFixed(2)}+(jHash-0.5)*0.026;\n        behind=mix(behind,vec3(0.20,0.15,0.125),frostVeil);\n        vec3 outgoingLight=mix(behind, diffuseColor.rgb*matcapColor.rgb, ${matcapWeight});  // el fondo domina, difuminado\n        outgoingLight+=fres*vec3(0.97,0.92,0.85)*(${(TIER.quality==='low'?.38:.28).toFixed(2)}+uHover*0.9);     // rim cálido (antes blanco-cian), se dispara al hover\n        outgoingLight+=uNeon*(${(TIER.quality==='low'?.1:.075).toFixed(3)}+uHover*0.45)*${neonStrength};          // tinte de etapa SIEMPRE presente, se dispara al hover\n        diffuseColor.a=mix(diffuseColor.a,0.92,uHover);                  // más opaca/visible al hover\n        `\n      );\n    };\n    m.userData.uHover=uHover;\n    return m;\n  }\n  const sharedMatcap=makeMatcap();\n  const plateGeo=new RoundedBoxGeometry(3.1,3.8,0.22,4,0.16);\n  const plateMaterials=[];\n  const plates=STAGES.map((s)=>{\n    const material=frostGlass(sharedMatcap,s.color);\n    plateMaterials.push(material);\n    const plate=new THREE.Mesh(plateGeo,material);\n    plate.position.set(s.x, s.y, 0);\n    scene.add(plate);\n    return plate;\n  });\n  const caseGeo=PAIR_SIDE ? new RoundedBoxGeometry(2.6,3.0,0.22,4,0.16) : new RoundedBoxGeometry(4.2,3.0,0.22,4,0.16);\n  const casePlates=CASES.map((c)=>{\n    const material=frostGlass(sharedMatcap,c.color);\n    plateMaterials.push(material);   // mismo array: resizeBgRT/renderBackdrop actualizan TODOS los materiales frosted\n    const plate=new THREE.Mesh(caseGeo,material);\n    plate.position.set(c.x, c.y, 0);\n    if(!PAIR_SIDE)plate.scale.setScalar(.82);\n    scene.add(plate);\n    return plate;\n  });\n  const allPlates=[...plates,...casePlates];\n\n  const DIAL_R=0.85, DIAL_TICKS=56, DIAL_MAJOR_EVERY=7;\n  function makeDialRing(color){\n    const geo=new THREE.TorusGeometry(DIAL_R,0.028,10,56);\n    const mat=frostGlass(sharedMatcap,color,{matcapWeight:0.32,neonStrength:0.7});\n    plateMaterials.push(mat);\n    return new THREE.Mesh(geo,mat);\n  }\n  function makeDialTicks(){\n    const geo=new THREE.BoxGeometry(0.016,0.09,0.018);\n    geo.translate(0,0.045,0);\n    const mat=new THREE.MeshMatcapMaterial({matcap:sharedMatcap,color:0x8a8078,transparent:true,opacity:0.85});\n    const mesh=new THREE.InstancedMesh(geo,mat,DIAL_TICKS);\n    const dummy=new THREE.Object3D();\n    for(let i=0;i<DIAL_TICKS;i++){\n      const a=(i/DIAL_TICKS)*Math.PI*2;\n      const isMajor=i%DIAL_MAJOR_EVERY===0;\n      dummy.position.set(Math.cos(a)*DIAL_R, Math.sin(a)*DIAL_R, 0);\n      dummy.rotation.set(0,0,a-Math.PI/2);\n      dummy.scale.set(1, isMajor?1.6:1, 1);\n      dummy.updateMatrix();\n      mesh.setMatrixAt(i,dummy.matrix);\n    }\n    mesh.instanceMatrix.needsUpdate=true;\n    return mesh;\n  }\n  function makeDialNeedle(color, angle){\n    const geo=new THREE.BoxGeometry(0.016,0.09,0.018);\n    geo.translate(0,0.045,0);\n    const mat=new THREE.MeshMatcapMaterial({matcap:sharedMatcap,color,transparent:true,opacity:0.95});\n    const mesh=new THREE.Mesh(geo,mat);\n    mesh.position.set(Math.cos(angle)*DIAL_R, Math.sin(angle)*DIAL_R, 0);\n    mesh.rotation.z=angle-Math.PI/2;\n    mesh.scale.set(1.6,2.1,1.6);\n    return mesh;\n  }\n  const cloudGroups=CLOUD_STATS.map((s,i)=>{\n    const group=new THREE.Group();\n    const spin=new THREE.Group();\n    spin.add(makeDialRing(s.color));\n    spin.add(makeDialTicks());\n    spin.add(makeDialNeedle(s.color, Math.PI*0.35+i*1.7));   // ángulo distinto por número, no alineados\n    group.add(spin);\n    group.userData.spin=spin;\n    group.userData.spinSpeed=0.045*(i%2===0?1:-1);   // sentido alternado por número, no todos iguales\n    group.position.set(s.x, s.y, 0);\n    if(!PAIR_SIDE) group.scale.setScalar(1.2);\n    scene.add(group);\n    return group;\n  });\n\n  const bodyTextMat=new THREE.MeshBasicMaterial({color:0xf2efe9, toneMapped:false, transparent:true});\n  const eyebrowTextMat=new THREE.MeshBasicMaterial({color:0xff6a2b, toneMapped:false, transparent:true});\n  const stepTextMat=new THREE.MeshBasicMaterial({color:0x4dffd0, toneMapped:false, transparent:true});\n  const BODY_TEXT_CURVES=TIER.quality==='low'?3:8;\n  const DISPLAY_TEXT_CURVES=TIER.quality==='low'?4:14;\n  const DISPLAY_TEXT_BEVEL=TIER.quality!=='low';\n  new FontLoader().load('https://cdn.jsdelivr.net/npm/three@0.160.0/examples/fonts/helvetiker_bold.typeface.json', (font)=>{\n  function addTextHaze(parent,source){\n    const hazeColor=new THREE.Color();\n    hazeColor.setRGB(...(TIER.quality==='low'?[0.95,0.82,0.68]:[3.2,2.8,2.45]));\n    const hazeMat=new THREE.MeshBasicMaterial({color:hazeColor,toneMapped:false,transparent:true,\n      opacity:TIER.quality==='low'?0.06:0.22,blending:THREE.AdditiveBlending,\n      depthWrite:false,depthTest:false});\n    const haze=new THREE.Mesh(source.geometry,hazeMat);\n    haze.position.copy(source.position); haze.rotation.copy(source.rotation); haze.scale.copy(source.scale);\n    haze.scale.multiplyScalar(TIER.quality==='low'?1.008:1.003);\n    haze.renderOrder=1; source.renderOrder=2;\n    parent.add(haze);\n  }\n  function wrapText(text,size,maxWidth){\n    text=text.replace(/×/g,'x');\n    const words=text.split(' '); const lines=[]; let current='';\n    const glyphs=font.data.glyphs,resolution=font.data.resolution||1000;\n    const measure=line=>[...line].reduce((sum,char)=>{\n      const glyph=glyphs[char]||glyphs['?'];return sum+(glyph?.ha||resolution*.5)*size/resolution;\n    },0);\n    for(const word of words){\n      const test=current?current+' '+word:word;\n      const w=measure(test);\n      if(w>maxWidth && current){ lines.push(current); current=word; }\n      else current=test;\n    }\n    if(current) lines.push(current);\n    return lines;\n  }\n  function addTextLines(parent,mat,lines,size,lineHeight,topY,maxW,z){\n    const geos=lines.map((line,li)=>{\n      const geo=new TextGeometry(line,{font,size,height:0.01,curveSegments:BODY_TEXT_CURVES,bevelEnabled:false});\n      geo.computeBoundingBox();\n      const w=geo.boundingBox.max.x-geo.boundingBox.min.x;\n      geo.translate(-w/2,0,0);                                    // centrado horizontal, ANTES de escalar\n      if(w>maxW){ const s=maxW/w; geo.scale(s,s,1); }              // clamp de ancho baked-in\n      geo.translate(0, topY-li*lineHeight, z);                     // posición final de esta línea\n      return geo;\n    });\n    const merged=mergeGeometries(geos,false);\n    geos.forEach(g=>g.dispose());\n    parent.add(new THREE.Mesh(merged,mat.clone()));\n  }\n  STAGES.forEach((s,i)=>{\n      const plate=plates[i];\n      const titleMat=new THREE.MeshBasicMaterial({color:0xf2efe9, toneMapped:false, transparent:true});\n      const lineHeight=0.40, size=0.27, totalH=(s.lines.length-1)*lineHeight, titleTopY=1.15;\n      s.lines.forEach((line,li)=>{\n        const geo=new TextGeometry(line,{font,size,height:0.1,curveSegments:DISPLAY_TEXT_CURVES,\n          bevelEnabled:DISPLAY_TEXT_BEVEL,bevelThickness:0.014,bevelSize:0.01,bevelSegments:3});\n        geo.computeBoundingBox();\n        const w=geo.boundingBox.max.x-geo.boundingBox.min.x;\n        geo.translate(-w/2,0,0);          // centrado horizontal\n        const mesh=new THREE.Mesh(geo,titleMat);\n        const maxW=2.6;                    // nunca más ancho que la placa (3.1) — clamp de seguridad\n        if(w>maxW) mesh.scale.setScalar(maxW/w);\n        mesh.position.set(0, titleTopY+totalH/2-li*lineHeight, 0.15);   // apenas saliente de la cara del vidrio\n        plate.add(mesh);\n        addTextHaze(plate,mesh);\n      });\n      addTextLines(plate, stepTextMat, [s.step], 0.13, 0, 0.55, 2.4, 0.15);\n      const bodyLines=wrapText(s.caption, 0.15, 2.6);\n      addTextLines(plate, bodyTextMat, bodyLines, 0.15, 0.225, 0.28, 2.6, 0.15);\n    });\n    CASES.forEach((c,i)=>{\n      const plate=casePlates[i];\n      const hookMat=new THREE.MeshBasicMaterial({color:0xf2efe9, toneMapped:false, transparent:true});   // sólido, mismo criterio/razón que titleMat arriba\n      const geo=new TextGeometry(c.hook,{font,size:0.46,height:0.13,curveSegments:DISPLAY_TEXT_CURVES,\n        bevelEnabled:DISPLAY_TEXT_BEVEL,bevelThickness:0.018,bevelSize:0.012,bevelSegments:3});\n      geo.computeBoundingBox();\n      const bb0=geo.boundingBox;\n      const w=bb0.max.x-bb0.min.x;\n      geo.translate(-(bb0.min.x+bb0.max.x)/2, -(bb0.min.y+bb0.max.y)/2, 0);\n      const mesh=new THREE.Mesh(geo,hookMat);\n      const maxW=PAIR_SIDE?2.1:3.5;        // placa angosta (2.6) o ancha (4.2) — clamp con margen\n      if(w>maxW) mesh.scale.setScalar(maxW/w);\n      mesh.position.set(0,0.62,0.15);      // arriba, deja lugar real debajo para tema+nombre+caption\n      plate.add(mesh);\n      addTextHaze(plate,mesh);\n      addTextLines(plate, eyebrowTextMat, [`${c.theme} — ${c.name}`], 0.115, 0, 0.1, PAIR_SIDE?2.15:3.6, 0.15);\n      const bodyLines=wrapText(c.caption, 0.13, PAIR_SIDE?2.15:3.6);\n      addTextLines(plate, bodyTextMat, bodyLines, 0.13, 0.195, -0.2, PAIR_SIDE?2.15:3.6, 0.15);\n    });\n    CLOUD_STATS.forEach((s,i)=>{\n      const group=cloudGroups[i];\n      const hookMat=new THREE.MeshBasicMaterial({color:0xf2efe9, toneMapped:false, transparent:true});   // sólido, mismo criterio/razón que titleMat arriba\n      const cloudHookSize=PAIR_SIDE?0.38:0.44;\n      const cloudLabelSize=PAIR_SIDE?0.09:0.11;\n      const cloudBodySize=PAIR_SIDE?0.095:0.12;\n      const cloudLineHeight=PAIR_SIDE?0.13:0.16;\n      const geo=new TextGeometry(s.hook,{font,size:cloudHookSize,height:0.12,curveSegments:DISPLAY_TEXT_CURVES,\n        bevelEnabled:DISPLAY_TEXT_BEVEL,bevelThickness:0.018,bevelSize:0.012,bevelSegments:3});\n      geo.computeBoundingBox();\n      const bb0=geo.boundingBox;\n      const w=bb0.max.x-bb0.min.x;\n      geo.translate(-(bb0.min.x+bb0.max.x)/2, -(bb0.min.y+bb0.max.y)/2, 0);\n      const mesh=new THREE.Mesh(geo,hookMat);\n      const maxW=1.3;    // cabe con margen dentro del radio interno del dial (~1.64 de diámetro libre)\n      if(w>maxW) mesh.scale.setScalar(maxW/w);\n      mesh.position.z=0.05;   // apenas adelante del plano del anillo/ticks\n      group.add(mesh);\n      addTextHaze(group,mesh);\n      addTextLines(group, eyebrowTextMat, [s.label], cloudLabelSize, 0, -1.15, 2.0, 0.05);\n      const bodyLines=wrapText(s.caption, cloudBodySize, 2.0);\n      addTextLines(group, bodyTextMat, bodyLines, cloudBodySize, cloudLineHeight, -1.32, 2.0, 0.05);\n    });\n    registerFadeGroups(plates); registerFadeGroups(casePlates); registerFadeGroups(cloudGroups);\n    document.documentElement.classList.remove('webgl-failed');\n    document.documentElement.classList.add('webgl-ready');\n  }, undefined, ()=>{\n    document.documentElement.classList.add('webgl-failed');\n  });\n\n  let bgRT=new THREE.WebGLRenderTarget(2,2,{depthBuffer:false});\n  function resizeBgRT(){\n    const backdropScale=TIER.quality==='low'?.55:1;\n    const w=Math.max(2,Math.round((canvas.width||2)*backdropScale));\n    const h=Math.max(2,Math.round((canvas.height||2)*backdropScale));\n    if(bgRT.width!==w||bgRT.height!==h){ bgRT.setSize(w,h);\n      plateMaterials.forEach(m=>{ if(m.userData.shader){m.userData.shader.uniforms.uResolution.value.set(w,h);\n        m.userData.shader.uniforms.uViewportResolution.value.set(canvas.width||2,canvas.height||2);} }); }\n  }\n  function renderBackdrop(){\n    const backdropConsumers=[...allPlates,...cloudGroups];\n    backdropConsumers.forEach(p=>p.visible=false);\n    renderer.setRenderTarget(bgRT); renderer.render(scene,camera); renderer.setRenderTarget(null);\n    backdropConsumers.forEach(p=>p.visible=true);\n    plateMaterials.forEach(m=>{ if(m.userData.shader){ m.userData.shader.uniforms.tBackdrop.value=bgRT.texture;\n      m.userData.shader.uniforms.uResolution.value.set(bgRT.width,bgRT.height);\n      m.userData.shader.uniforms.uViewportResolution.value.set(canvas.width||2,canvas.height||2); } });\n  }\n\n  const threadUniforms={ uTime:{value:0},uReadFocus:{value:0} };\n  const threadMat=new THREE.ShaderMaterial({\n    uniforms:threadUniforms, transparent:true, depthWrite:false, toneMapped:false,\n    vertexShader:`varying vec2 vUv; void main(){ vUv=uv; gl_Position=projectionMatrix*modelViewMatrix*vec4(position,1.0); }`,\n    fragmentShader:`\n      uniform float uTime,uReadFocus; varying vec2 vUv;\n      void main(){\n        float dash=mix(0.38,step(0.5,fract(vUv.x*46.0-uTime*1.4)),${(TIER.quality==='low'?0:1).toFixed(1)});\n        float ring=mix(0.10,step(0.82,fract(vUv.y*5.0+uTime*0.15)),${(TIER.quality==='low'?0:1).toFixed(1)});\n        float base=${(TIER.quality==='low'?.38:.32).toFixed(2)};\n        float e=base+dash*${(TIER.quality==='low'?.7:.62).toFixed(2)}+ring*0.3;\n        vec3 col=mix(vec3(0.85,0.38,0.16),vec3(1.0,0.78,0.58),dash*0.7+ring*0.4);  // naranja de marca (antes cian)\n        float readingFade=1.0-uReadFocus*${(TIER.quality==='low'?.2:.6).toFixed(2)};\n        gl_FragColor=vec4(col*e*readingFade, clamp(e,0.12,0.85)*readingFade);\n      }`\n  });\n  const thread=new THREE.Mesh(new THREE.TubeGeometry(threadCurve,220,0.016,8,false),threadMat);\n  scene.add(thread);\n\n  const DIRECT_MOBILE=TIER.quality==='low';\n  const composer=DIRECT_MOBILE?null:new EffectComposer(renderer);\n  if(composer){\n    composer.addPass(new RenderPass(scene,camera));\n    if(TIER.bloom>0&&!new URLSearchParams(location.search).has('nobloom')){\n      composer.addPass(new UnrealBloomPass(new THREE.Vector2(innerWidth,innerHeight),TIER.bloom,0.38,0.96));\n    }\n    composer.addPass(new SMAAPass(innerWidth*renderer.getPixelRatio(), innerHeight*renderer.getPixelRatio()));\n    composer.addPass(new OutputPass());\n  }\n  function renderScene(){if(DIRECT_MOBILE)renderer.render(scene,camera);else composer.render();}\n\n  function aspectZScale(){ return Math.min(PAIR_SIDE?2.3:2.55, Math.max(1, 1.6/camera.aspect)); }\n  function fit(){renderer.setSize(innerWidth,innerHeight);if(composer)composer.setSize(innerWidth,innerHeight);\n    camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix(); resizeBgRT();}\n  fit(); addEventListener('resize',fit);\n  let pairResizeTimer;\n  addEventListener('resize',()=>{\n    clearTimeout(pairResizeTimer);\n    pairResizeTimer=setTimeout(()=>{\n      if(isPairSide()!==PAIR_SIDE) location.reload();\n    },180);\n  });\n\n  const camLook=new THREE.Vector3(0,0,0);\n  const lastPlateY=CASES.at(-1).y;   // el eje ahora termina en el último caso de Work, no en Method\n  const lerp=(a,b,k)=>a+(b-a)*k;\n  const heroEl=document.querySelector('.hero');\n  const leadInEl=document.querySelector('.lead-in');\n  const methodEl=document.getElementById('method');\n  const workEl=document.getElementById('work');\n  const teamEl=document.getElementById('team');\n  const scrollCueEl=document.querySelector('.scroll-cue');\n  const CUE_MIN_GAP=24;   // px de aire mínimo que debe quedar entre el CTA y el cue\n  let cueBlockedByLayout=false;\n  function checkHeroCueCollision(){\n    const heroTail=document.querySelector('.hero-proof')||document.querySelector('.hero .cta');\n    if(!heroTail||!scrollCueEl) return;\n    const gap=scrollCueEl.getBoundingClientRect().top - heroTail.getBoundingClientRect().bottom;\n    cueBlockedByLayout = gap < CUE_MIN_GAP;\n    scrollCueEl.style.opacity=(scrollY>heroEl.offsetHeight*.35||cueBlockedByLayout)?0:1;\n  }\n  checkHeroCueCollision();\n  addEventListener('resize', checkHeroCueCollision);\n  document.fonts && document.fonts.ready && document.fonts.ready.then(checkHeroCueCollision);\n  const CLOUD_Y_START=Math.max(...CLOUD_STATS.map(s=>s.y)); // dial más superficial (primero en aparecer)\n  const CLOUD_Y_END=Math.min(...CLOUD_STATS.map(s=>s.y));   // dial más profundo\n  const METHOD_Y_END=STAGES.at(-1).y; // última placa de Method\n  const CLOUD_APPROACH_FRAC=PAIR_SIDE?0.25:0.08;\n  const HERO_CAM_Y=1.6;\n  let progress=0, camTargetY=HERO_CAM_Y, camTargetZ=9, camLookTargetY=0, mainCanvasActive=true, workRelease=1;\n  const meanY=items=>items.reduce((sum,item)=>sum+item.y,0)/items.length;\n  const MOBILE_CLOUD_SCENES=[meanY(CLOUD_STATS.slice(0,2)),meanY(CLOUD_STATS.slice(2,4))];\n  const MOBILE_METHOD_SCENES=[meanY(STAGES.slice(0,2)),meanY(STAGES.slice(1,3))];\n  const MOBILE_WORK_GROUPS=[[0,1],[2,5]];\n  const MOBILE_SELECTED_CASES=MOBILE_WORK_GROUPS.flat();\n  const MOBILE_WORK_SCENES=MOBILE_WORK_GROUPS.map((ids,scene)=>ids.reduce((sum,i)=>sum+CASES[i].y,0)/ids.length-(scene===1?.5:0));\n  const MOBILE_JOURNEY_SCENES=[0,...MOBILE_CLOUD_SCENES,...MOBILE_METHOD_SCENES,...MOBILE_WORK_SCENES];\n  function mobileScene(p,ys){\n    p=Math.max(0,Math.min(1,p));\n    if(ys.length<2)return ys[0]??0;\n    const scaled=p*(ys.length-1),i=Math.min(ys.length-2,Math.floor(scaled));\n    let q=scaled-i;q=q*q*(3-2*q);\n    return lerp(ys[i],ys[i+1],q);\n  }\n  function updateFromScroll(){\n    // The 3D journey (particles ordering + camera travel) begins only after the\n    // hero AND the full-screen .lead-in intro are scrolled past, so the opaque\n    // lead-in never overlaps the dials and the camera stays in sync.\n    const preJourney=heroEl.offsetHeight+(leadInEl?leadInEl.offsetHeight:0);\n    const preMethodRange=Math.max(1, preJourney-innerHeight*0.3);\n    uniforms.uOrder.value=Math.max(0,Math.min((scrollY-(leadInEl?leadInEl.offsetHeight:0))/preMethodRange,1));\n    const travelY=!PAIR_SIDE?scrollY+innerHeight*.5:scrollY;\n    const journeyTop=preJourney;\n    const journeyBottom=workEl.offsetTop+workEl.offsetHeight;\n    document.body.classList.toggle('journey-active',travelY>=journeyTop&&travelY<journeyBottom);\n    const journeyScrollable=Math.max(1,!PAIR_SIDE?journeyBottom-journeyTop:journeyBottom-innerHeight-journeyTop);\n    progress=Math.max(0,Math.min(1,(travelY-journeyTop)/journeyScrollable));\n    const zProgress=Math.min(1, progress*14);\n    camTargetZ=lerp(9, 5.2, zProgress)*aspectZScale();\n    const methodTop=methodEl.offsetTop, workTop=workEl.offsetTop;\n    const mobileReadableEnd=journeyBottom-innerHeight*.42;\n    const mobileExitEnd=journeyBottom-innerHeight*.12;\n    if(!PAIR_SIDE){\n      if(travelY<=mobileReadableEnd){\n        workRelease=1;\n        const p=Math.max(0,Math.min(1,(travelY-journeyTop)/Math.max(1,mobileReadableEnd-journeyTop)));\n        camLookTargetY=mobileScene(p,MOBILE_JOURNEY_SCENES);\n      }else{\n        const exitP=Math.max(0,Math.min(1,(travelY-mobileReadableEnd)/Math.max(1,mobileExitEnd-mobileReadableEnd)));\n        workRelease=1-exitP;\n        camLookTargetY=lerp(MOBILE_JOURNEY_SCENES.at(-1),lastPlateY,exitP);\n      }\n    }else if(travelY<methodTop){\n      const p=Math.max(0,Math.min(1,(travelY-journeyTop)/Math.max(1,methodTop-journeyTop)));\n      camLookTargetY = p<CLOUD_APPROACH_FRAC\n        ? lerp(0, CLOUD_Y_START, p/CLOUD_APPROACH_FRAC)\n        : (!PAIR_SIDE\n          ? mobileScene((p-CLOUD_APPROACH_FRAC)/(1-CLOUD_APPROACH_FRAC),MOBILE_CLOUD_SCENES)\n          : lerp(CLOUD_Y_START, CLOUD_Y_END, (p-CLOUD_APPROACH_FRAC)/(1-CLOUD_APPROACH_FRAC)));\n    }else if(travelY<workTop){\n      const p=Math.max(0,Math.min(1,(travelY-methodTop)/Math.max(1,workTop-methodTop)));\n      camLookTargetY=!PAIR_SIDE?mobileScene(p,MOBILE_METHOD_SCENES):lerp(CLOUD_Y_END, METHOD_Y_END, p);\n    }else{\n      const workReadableEnd=journeyBottom-innerHeight*1.30;\n      const workExitEnd=journeyBottom-innerHeight*.82;\n      if(travelY<=workReadableEnd){\n        workRelease=1;\n        const p=Math.max(0,Math.min(1,(travelY-workTop)/Math.max(1,workReadableEnd-workTop)));\n        camLookTargetY=!PAIR_SIDE?mobileScene(p,MOBILE_WORK_SCENES):lerp(METHOD_Y_END,lastPlateY,p);\n      }else{\n        const exitP=Math.max(0,Math.min(1,(travelY-workReadableEnd)/Math.max(1,workExitEnd-workReadableEnd)));\n        workRelease=1-exitP;\n        camLookTargetY=lastPlateY;\n      }\n    }\n    camTargetY=progress>0 ? camLookTargetY+0.32 : HERO_CAM_Y;\n    if(scrollCueEl) scrollCueEl.style.opacity=(scrollY>heroEl.offsetHeight*0.35||cueBlockedByLayout)?0:1;\n    const canvasFadeStart=journeyBottom-innerHeight*.46;\n    const canvasFadeDist=innerHeight*.34;\n    const canvasFadeP=Math.max(0,Math.min(1,(scrollY-canvasFadeStart)/canvasFadeDist));\n    canvas.style.opacity=String(1-canvasFadeP);\n    mainCanvasActive=canvasFadeP<0.995;\n    canvas.style.visibility=mainCanvasActive?'visible':'hidden';\n  }\n  addEventListener('scroll',updateFromScroll,{passive:true});\n\n  const ray=new THREE.Raycaster(), ndc=new THREE.Vector2(), plane=new THREE.Plane(new THREE.Vector3(0,0,1),0), hit=new THREE.Vector3();\n  let pointerX=0, hoveredPlate=null;\n  function setMouse(cx,cy){\n    ndc.set((cx/innerWidth)*2-1,-(cy/innerHeight)*2+1); ray.setFromCamera(ndc,camera);\n    ray.ray.intersectPlane(plane,hit); if(hit) uniforms.uMouse.value.copy(hit); uniforms.uForce.value=1.1;\n    pointerX=(cx/innerWidth-0.5);\n    const hits=ray.intersectObjects(allPlates,false);\n    hoveredPlate=hits.length?hits[0].object:null;\n  }\n  if(!TOUCH) addEventListener('mousemove',e=>setMouse(e.clientX,e.clientY));\n\n  const hazeEl=document.querySelector('.cursor-haze');\n  if(!TOUCH && !REDUCE){\n    addEventListener('mousemove',e=>{\n      hazeEl.style.transform=`translate(${e.clientX}px,${e.clientY}px) translate(-50%,-50%)`;\n      hazeEl.classList.add('on');\n    });\n    addEventListener('mouseleave',()=>hazeEl.classList.remove('on'));\n  }\n\n  const stageProximity=new Array(STAGES.length).fill(0);\n  const caseProximity=new Array(CASES.length).fill(0);\n  const cloudProximity=new Array(CLOUD_STATS.length).fill(0);\n  function updateProximity(){\n    const stageRange=PAIR_SIDE?5:7;\n    const caseRange=PAIR_SIDE?5:5.8;\n    for(let i=0;i<STAGES.length;i++) stageProximity[i]=Math.max(0,Math.min(1,1-Math.abs(camLook.y-STAGES[i].y)/stageRange));\n    for(let i=0;i<CASES.length;i++) caseProximity[i]=Math.max(0,Math.min(1,1-Math.abs(camLook.y-CASES[i].y)/caseRange));\n    const cloudRange=PAIR_SIDE?5:6;\n    for(let i=0;i<CLOUD_STATS.length;i++) cloudProximity[i]=Math.max(0,Math.min(1,1-Math.abs(camLook.y-CLOUD_STATS[i].y)/cloudRange));\n  }\n  const FOCUS_FLOOR=TIER.quality==='low'?0:0.02;\n  function registerFadeGroups(list){\n    list.forEach(root=>{\n      const mats=[];\n      root.traverse(o=>{ if(o.material){ o.material.transparent=true; mats.push({m:o.material, base:o.material.opacity}); } });\n      root.userData.fadeMats=mats;\n    });\n  }\n  function applyFocusFade(){\n    const journeyPresence=Math.max(0,Math.min(1,(-camLook.y-3)/3));\n    const fade=(root,prox,release=1)=>{\n      const mats=root.userData.fadeMats; if(!mats) return;\n      const focusCurve=!PAIR_SIDE?Math.pow(prox,1.05):(TIER.quality==='low'?Math.pow(prox,1.25):Math.pow(prox,1.55));\n      const f=(FOCUS_FLOOR+(1-FOCUS_FLOOR)*focusCurve)*journeyPresence*release;\n      for(const e of mats) e.m.opacity=e.base*f;\n    };\n    if(!PAIR_SIDE){\n      const mobileCaseFocus=prox=>{let q=Math.max(0,Math.min(1,(prox-.25)/.4));return q*q*(3-2*q)};\n      plates.forEach((p,i)=>{p.userData.mobileActive=true;fade(p,stageProximity[i])});\n      casePlates.forEach((p,i)=>{const active=MOBILE_SELECTED_CASES.includes(i);p.userData.mobileActive=active;\n        if(!active&&p.material.userData.uHover)p.material.userData.uHover.value=0;\n        fade(p,active?mobileCaseFocus(caseProximity[i]):0,workRelease)});\n      cloudGroups.forEach((g,i)=>{g.userData.mobileActive=true;fade(g,cloudProximity[i])});\n    }else{\n      plates.forEach((p,i)=>fade(p,stageProximity[i]));\n      casePlates.forEach((p,i)=>fade(p,caseProximity[i],workRelease));\n      cloudGroups.forEach((g,i)=>fade(g,cloudProximity[i]));\n    }\n  }\n\n  const clock=new THREE.Clock();\n  let lastRenderMs=performance.now();\n  function render(now=performance.now()){\n    const dt=Math.min(.05,Math.max(.001,(now-lastRenderMs)*.001));lastRenderMs=now;\n    const cameraEase=REDUCE?1:1-Math.exp(-CAM_RESPONSE*dt);\n    const t=REDUCE?0:clock.getElapsedTime();\n    uniforms.uTime.value=t; uniforms.uForce.value*=0.94;\n    threadUniforms.uTime.value=t;\n    camera.position.y+=(camTargetY-camera.position.y)*cameraEase;\n    camera.position.z+=(camTargetZ-camera.position.z)*cameraEase;\n    camera.position.x+=((progress<0.14?pointerX*1.4:0)-camera.position.x)*cameraEase;\n    camLook.y+=(camLookTargetY-camLook.y)*cameraEase;\n    camera.lookAt(camLook);\n    cloudGroups.forEach(g=>{ g.userData.spin.rotation.z=REDUCE?0:t*g.userData.spinSpeed; });\n    updateProximity();\n    const readFocus=Math.max(...stageProximity,...caseProximity,...cloudProximity);\n    uniforms.uReadFocus.value=readFocus;\n    threadUniforms.uReadFocus.value=readFocus;\n    applyFocusFade();\n    plates.forEach((p,i)=>{\n      const target=TOUCH ? (p.userData.mobileActive===false?0:stageProximity[i]*0.55) : ((p===hoveredPlate)?stageProximity[i]:0);\n      p.material.userData.uHover.value+=(target-p.material.userData.uHover.value)*cameraEase; });\n    casePlates.forEach((p,i)=>{\n      const target=TOUCH ? (p.userData.mobileActive===false?0:caseProximity[i]*0.55) : ((p===hoveredPlate)?caseProximity[i]:0);\n      p.material.userData.uHover.value+=(target-p.material.userData.uHover.value)*cameraEase; });\n    if(mainCanvasActive){\n      renderBackdrop();\n      renderScene();\n    }\n    if(REDUCE) setTimeout(()=>requestAnimationFrame(render),100);\n    else requestAnimationFrame(render);\n  }\n  updateFromScroll();\n  camera.lookAt(camLook); updateProximity();   // evita el flash de posición (0,0) en el primer frame\n  render();\n  </script>\n  <script>\n  (()=>{\n    const emit=(name,detail={})=>{\n      window.dataLayer?.push({event:name,...detail});\n      dispatchEvent(new CustomEvent('amir:conversion',{detail:{event:name,...detail}}));\n    };\n    addEventListener('click',event=>{\n      const target=event.target.closest('[data-event]');\n      if(target) emit(target.dataset.event,{label:target.textContent.trim()});\n    });\n\n    const canvas=document.getElementById('tail-liquid');\n    const startEl=document.querySelector('.expertise');\n    const footer=document.querySelector('.site-footer');\n    if(!canvas||!startEl||!footer) return;\n    const reduce=matchMedia('(prefers-reduced-motion:reduce)').matches;\n    let gl,program,buffer,uniforms,initialized=false,running=false,active=false;\n    let pointerX=.5,pointerY=.5,targetX=.5,targetY=.5,pointerVX=0,pointerVY=0,pointerImpulse=0;\n    let textDX=0,textDY=0,targetTextDX=0,targetTextDY=0;\n    let lastScroll=scrollY,scrollVelocity=0,scrollImpulse=0,lastFrame=0;\n\n    function shader(type,source){\n      const value=gl.createShader(type); gl.shaderSource(value,source); gl.compileShader(value);\n      if(!gl.getShaderParameter(value,gl.COMPILE_STATUS)) throw new Error(gl.getShaderInfoLog(value));\n      return value;\n    }\n    function init(){\n      if(initialized) return true;\n      initialized=true;\n      gl=canvas.getContext('webgl',{alpha:true,antialias:false,depth:false,premultipliedAlpha:true});\n      if(!gl) return false;\n      const vertex=`attribute vec2 p;void main(){gl_Position=vec4(p,0.0,1.0);}`;\n      const fragment=`\n        precision highp float;\n        uniform vec2 r,m,d;\n        uniform float t,v,i,s;\n        float h(vec2 p){return fract(sin(dot(p,vec2(127.1,311.7)))*43758.5453);}\n        float n(vec2 p){vec2 i=floor(p),f=fract(p);f=f*f*(3.0-2.0*f);\n          return mix(mix(h(i),h(i+vec2(1,0)),f.x),mix(h(i+vec2(0,1)),h(i+vec2(1,1)),f.x),f.y);}\n        float fbm(vec2 p){float f=0.0,a=.5;for(int i=0;i<5;i++){f+=a*n(p);p=p*2.03+vec2(17.1,9.2);a*=.5;}return f;}\n        void main(){\n          vec2 uv=gl_FragCoord.xy/r;\n          vec2 p=uv-.5;p.x*=r.x/r.y;\n          float speed=clamp(abs(v)*.024+s,0.0,1.0);\n          vec2 flow=vec2(t*.045+v*.0024,-t*.075);\n          vec2 mp=m-.5;mp.x*=r.x/r.y;\n          vec2 dir=d;dir.x*=r.x/r.y;\n          float dirLen=max(length(dir),.001);\n          vec2 axis=dir/dirLen;\n          vec2 rel=p-mp;\n          float along=dot(rel,axis),across=dot(rel,vec2(-axis.y,axis.x));\n          float trail=exp(-abs(across)*13.0)*exp(-max(along,0.0)*4.0)*exp(-max(-along,0.0)*1.2)*i;\n          float rings=sin(length(rel)*46.0-i*3.0)*exp(-length(rel)*5.0)*i;\n          float scrollWave=sin((p.y+t*.55)*18.0+p.x*5.0)*speed*.12;\n          vec2 displacement=axis*trail*.48+normalize(rel+vec2(.001))*rings*.08+vec2(scrollWave*.18,v*.00055);\n          float q=fbm((p+displacement)*2.15+flow);\n          float warped=fbm((p+displacement)*3.7+vec2(q,-q)*1.55-flow*.7);\n          float fine=fbm((p-displacement*.45)*7.4+vec2(warped,q)+flow*.35);\n          float wake=exp(-length(rel)*4.2)*(.28+i*.95)+trail*.9+max(rings,0.0)*.45;\n          float current=smoothstep(.32,.78,warped*.7+fine*.4+speed*.2);\n          float veil=smoothstep(.12,.82,q)*.58+current*.88+wake*.8;\n          vec3 ember=vec3(1.0,.22,.055);\n          vec3 cream=vec3(1.0,.69,.46);\n          vec3 color=mix(ember,cream,clamp(fine+wake*.45,0.0,1.0));\n          float edge=1.0-smoothstep(.38,.78,length(p));\n          float alpha=clamp(veil*.21*edge+wake*.095,0.0,.38);\n          gl_FragColor=vec4(color*alpha,alpha);\n        }`;\n      program=gl.createProgram();\n      gl.attachShader(program,shader(gl.VERTEX_SHADER,vertex));\n      gl.attachShader(program,shader(gl.FRAGMENT_SHADER,fragment));\n      gl.linkProgram(program);\n      if(!gl.getProgramParameter(program,gl.LINK_STATUS)) throw new Error(gl.getProgramInfoLog(program));\n      buffer=gl.createBuffer();gl.bindBuffer(gl.ARRAY_BUFFER,buffer);\n      gl.bufferData(gl.ARRAY_BUFFER,new Float32Array([-1,-1,3,-1,-1,3]),gl.STATIC_DRAW);\n      const position=gl.getAttribLocation(program,'p');gl.enableVertexAttribArray(position);gl.vertexAttribPointer(position,2,gl.FLOAT,false,0,0);\n      uniforms={r:gl.getUniformLocation(program,'r'),m:gl.getUniformLocation(program,'m'),d:gl.getUniformLocation(program,'d'),t:gl.getUniformLocation(program,'t'),v:gl.getUniformLocation(program,'v'),i:gl.getUniformLocation(program,'i'),s:gl.getUniformLocation(program,'s')};\n      resize(); return true;\n    }\n    function resize(){\n      if(!gl) return;\n      const scale=.65*Math.min(devicePixelRatio,1.25);\n      canvas.width=Math.max(2,Math.round(innerWidth*scale));canvas.height=Math.max(2,Math.round(innerHeight*scale));\n      gl.viewport(0,0,canvas.width,canvas.height);\n    }\n    function draw(now){\n      if(!active){running=false;return;}\n      running=true;\n      const dt=Math.min(.05,Math.max(.008,lastFrame?(now-lastFrame)*.001:.016));lastFrame=now;\n      const previousX=pointerX,previousY=pointerY;\n      const positionEase=1-Math.exp(-dt*6.5);\n      pointerX+=(targetX-pointerX)*positionEase;pointerY+=(targetY-pointerY)*positionEase;\n      const rawVX=(pointerX-previousX)/dt,rawVY=(pointerY-previousY)/dt;\n      const velocityEase=1-Math.exp(-dt*9);\n      pointerVX+=(rawVX-pointerVX)*velocityEase;pointerVY+=(rawVY-pointerVY)*velocityEase;\n      const energyTarget=Math.min(1.15,Math.hypot(rawVX,rawVY)*.34);\n      const energyEase=1-Math.exp(-dt*(energyTarget>pointerImpulse?7.5:2.2));\n      pointerImpulse+=(energyTarget-pointerImpulse)*energyEase;\n      scrollVelocity*=Math.exp(-dt*7.7);scrollImpulse*=Math.exp(-dt*4.4);\n      const textEase=1-Math.exp(-dt*5.8);\n      textDX+=(targetTextDX-textDX)*textEase;\n      textDY+=(targetTextDY+Math.max(-1.2,Math.min(1.2,scrollVelocity*.016))-textDY)*textEase;\n      document.body.style.setProperty('--liquid-dx',textDX.toFixed(2)+'px');\n      document.body.style.setProperty('--liquid-dy',textDY.toFixed(2)+'px');\n      document.body.style.setProperty('--liquid-px',(pointerX*100).toFixed(1)+'%');\n      document.body.style.setProperty('--liquid-py',((1-pointerY)*100).toFixed(1)+'%');\n      gl.useProgram(program);\n      gl.uniform2f(uniforms.r,canvas.width,canvas.height);\n      gl.uniform2f(uniforms.m,pointerX,pointerY);\n      gl.uniform2f(uniforms.d,pointerVX,pointerVY);\n      gl.uniform1f(uniforms.t,reduce?0:now*.001);\n      gl.uniform1f(uniforms.v,reduce?0:scrollVelocity);\n      gl.uniform1f(uniforms.i,reduce?0:Math.min(1.5,pointerImpulse));\n      gl.uniform1f(uniforms.s,reduce?0:Math.min(1,scrollImpulse));\n      gl.drawArrays(gl.TRIANGLES,0,3);\n      if(!reduce) requestAnimationFrame(draw); else running=false;\n    }\n    function updateActive(){\n      const start=startEl.offsetTop-innerHeight*.85;\n      const end=footer.offsetTop+footer.offsetHeight;\n      active=scrollY>=start&&scrollY<=end;\n      document.body.classList.toggle('tail-active',active);\n      if(active&&init()&&!running) requestAnimationFrame(draw);\n    }\n    addEventListener('scroll',()=>{\n      const next=scrollY,delta=next-lastScroll;scrollVelocity+=delta*.24;\n      scrollImpulse=Math.min(1,scrollImpulse+Math.abs(delta)*.006);lastScroll=next;updateActive();\n    },{passive:true});\n    addEventListener('mousemove',event=>{\n      targetX=event.clientX/innerWidth;targetY=1-event.clientY/innerHeight;\n      targetTextDX=(targetX-.5)*2.4;targetTextDY=(.5-targetY)*1.8;\n    },{passive:true});\n    addEventListener('resize',()=>{resize();updateActive()});\n    updateActive();\n  })();\n  </script>";
export default bodyHtml;
